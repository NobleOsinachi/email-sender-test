import open from 'open';

// Specify the URL or path you want to open in the browser
const urlToOpen = 'http://localhost:3000'; // Replace with your desired URL

// Open the URL in the default browser
open(urlToOpen);
